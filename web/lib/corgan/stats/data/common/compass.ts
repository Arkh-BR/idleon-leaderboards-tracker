// ===== COMPASS DATA STUB (Stage 3 will port real impl) =====
// Returns a safe 0 per-level value so dependent calculations short-circuit.
export function compassUpgPerLevel(_idx: number): number {
  return 0;
}
