// ===== W5 DIVINITY SYSTEM STUB (Stage 3 will port real impl) =====
import type { SaveData } from "../../../state";

export function hasBonusMajor(_charIdx: number, _bonusIdx: number, _saveData: SaveData): boolean {
  return false;
}
