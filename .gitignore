# Claude / IDE
.claude/

# Node / Next
node_modules/
.next/
out/
.vercel
*.log
.env*.local

# OS
.DS_Store
Thumbs.db

# IT source for reference during debugging — re-clone with:
# git clone --depth 1 https://github.com/Morta1/IdleonToolbox.git web/scripts/it-source
web/scripts/it-source/

# Corgan source for reference during the Drop Rate port — re-clone with:
# git clone --depth 1 https://github.com/Corgan/idleon-research-optimizer.git web/scripts/corgan-source
web/scripts/corgan-source/

# Reverse-engineering scratch files (e.g. raw N.js dump for grep work).
# Don't commit — fetch fresh from https://www.legendsofidleon.com/ytGl5oc/N.js if needed.
.Njs-investigation.js
*.Njs*

# Personal "Copy for Support" dumps used as one-shot validation against IT's
# live numbers. They contain account-identifying data — keep out of the repo.
data from suport*.json
data_from_suport*.json

# Corgan reference HTML used for layout matching
.corgan-*.html
.corgan-reference/
